Here’s a **detailed Replit prompt** you can use to guide your development team or LLM-powered coding assistant to execute the enhancement and debugging mandate you outlined:

---

# Replit Prompt: Functionality Expansion & UI Debugging

**Context:**
We are enhancing an application by **adding new functionality** while simultaneously **debugging UI/UX issues** and **eliminating errors** across the system. The goal is to deliver a production-grade, stable release with improved user experience and system resilience.

---

### **Primary Objectives**

1. **Functionality Expansion**

   * Implement new features aligned with the current roadmap.
   * Ensure CRUD operations, permissions, and data validation are fully covered.
   * Add robust error handling and fallbacks for edge cases.

2. **UI/UX Debugging**

   * Fix overlapping components, broken layouts, and z-index conflicts.
   * Validate responsiveness across all supported screen sizes (desktop, tablet, mobile).
   * Ensure consistency with design tokens, brand guidelines, and component library.
   * Validate accessibility (WCAG 2.1 AA compliance).

3. **System Error Remediation**

   * Debug and resolve all critical (P0/P1) errors.
   * Improve error messages for clarity and user-friendliness.
   * Prevent silent failures—ensure all operations return appropriate feedback.

---

### **Technical Requirements**

* **Cross-Browser Support:** Validate on Chrome, Firefox, Safari, and Edge.
* **Responsive Design:** Ensure fluid layouts and no overlaps at breakpoints.
* **Testing Coverage:**

  * Unit test coverage ≥ 95% for updated modules.
  * End-to-end smoke test coverage ≥ 90%.
  * No regressions in previously fixed issues.
* **Performance:**

  * p95 load times within benchmark targets.
  * Minimize blocking scripts and optimize rendering paths.

---

### **Acceptance Criteria**

* Zero open blocking (P0/P1) bugs at release.
* No overlapping UI elements on any supported viewport.
* All new features working correctly with complete CRUD flows.
* Accessibility validated (keyboard navigation, ARIA roles, contrast checks).
* Error logs are clean and actionable, with monitoring dashboards configured.

---

### **Deliverables**

* Updated **feature spec** and **UI spec**.
* Debugged and **refactored codebase** with clean commits.
* Test plan with coverage report and accessibility audit.
* Deployment-ready build with rollback plan.

---

💡 **Instruction to Replit/AI Agent:**
Generate clean, modular code that implements the new functionality while systematically debugging UI overlaps and runtime errors. Enforce best practices (linting, code reviews, feature flags). Provide test stubs, error handling improvements, and responsive design fixes. Ensure maintainability and scalability for future iterations.

---

Would you like me to **translate this into a runnable “Replit project setup prompt”** (with suggested file structure, dependencies, and starter scripts) — or keep it as a **strategic directive** for developers using Replit?
